[project]
name = "agent-0-react"
version = "0.1.0"
description = "Minimal ReAct weather agent using OpenAI + wttr.in"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "autogen>=0.13.4",
    "langgraph>=1.2.5",
    "mcp>=1.27.2",
    "openai",
    "python-dotenv",
]
